[project]
name = "seq-photo-compression"
version = "0.1.0"
description = "Sequential NEF compression experiment"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "opencv-python>=4.10",
]
